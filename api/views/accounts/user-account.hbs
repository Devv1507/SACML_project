
<div class="row">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-items-center">
                        {{account.name}} 
                        <a href="/api/v1/home/update/{{account.id}}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
                    </h4>
                    <p>{{account.email}}</p>
                    <form action="/api/v1/home/{{id}}?_method=DELETE" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </div>
            </div>
        </div>
</div>